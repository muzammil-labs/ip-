/** The only z-index values used anywhere in the app. No component sets a z-index outside this scale. */
export const LAYER = {
  header: 30,
  rail: 20,
  drawer: 40,
  sheet: 50,
  toast: 60,
} as const;
