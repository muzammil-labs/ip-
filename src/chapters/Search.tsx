import PriorArt from "../screens/PriorArt";

/** Case chapter 6: search (B2, B3). Temporarily re-exports the pre-redesign Prior art & TK screen; queries prefilled from the Case formula land in Phase 4. */
export default function Search() {
  return <PriorArt />;
}
