import ClaimCheck from "../screens/ClaimCheck";

/** Case chapter 5: say (B2, B3). Temporarily re-exports the pre-redesign Claim check screen; the version where category comes from the Case with no separate picker lands in Phase 4. */
export default function Say() {
  return <ClaimCheck />;
}
