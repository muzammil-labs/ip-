import Ask from "../screens/Ask";

/** #/ask (B2): the full-page Sahayak view, used for Kisan mode. Temporarily re-exports the existing Ask screen; the panel version (right drawer / bottom sheet, available on every page) is built in Phase 4. */
export default function AskPage() {
  return <Ask />;
}
