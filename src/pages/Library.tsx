import Sources from "../screens/Sources";

/**
 * #/library (B2). Temporarily re-exports the pre-redesign Sources screen. The real
 * Library (DataTable primitive, clause pages, legal time machine) is built in Phase 4.
 */
export default function Library() {
  return <Sources />;
}
