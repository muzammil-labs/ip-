# Translation TODO

Keys shipped with English copied into `hi.ts`/`hi.ts` because the string names a specific
statute section plus a legal-effect term (e.g. "Section 3(e) synergy data needed") and a
non-expert paraphrase risks changing its legal meaning across languages, per D0 rule 4 and
UI-6.5's rule that only the team's legal reviewer writes legal statements. Translate together
with a bilingual legal reviewer, not ad hoc.

## Phase 6

- `tkReadingClassical`, `tkReadingProprietary`, `tkReadingNewAsu` (`src/i18n/hi.ts`, `te.ts`) — the
  three TK-proximity legal-reading labels (UI-6.1). English text also used as the hi/te value.
