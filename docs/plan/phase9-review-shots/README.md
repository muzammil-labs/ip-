# Phase 9 review screenshots

Before/after screenshots for the Phase 9 UI polish PR, captured at 1440px with
`prefers-reduced-motion: reduce` (so scroll-reveal sections render statically).
"Before" is the state at the last pre-Phase-9 commit (`9c12f4d`, the Phase 8
merge); "after" is this branch. Referenced from the PR description; safe to
delete after the PR is reviewed.
